#!/usr/bin/perl
# Create Hash (create_hash.pl)
# 2013-09-25
# (q) benefit@kantsu

# Create Hash
%perl_hash = (
	browser => iceweasel,
# You can also use comma instead of arrow operator
	os , linux, 
);

# Print Hash element! :D
print "$perl_hash{'browser'}\n";
