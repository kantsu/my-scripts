#!/usr/bin/perl
# Perl until loop (until_loop.pl)
# 2013-09-26 - Samuli d[^_^]b
$until_loop=5;

# Definition of perl until loop
until ($until_loop==0) {
	print "UNTIL LOOP value is: ".$until_loop."\n";
	$until_loop--;
} 
